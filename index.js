'use strict';

//dependencies

/**
 * @description common HTTP responses for expressjs
 * @param  {Object} options options to configure express-respond functions
 * //options jsend,environment
 * @return {Function}       a valid expressjs middleware
 */
module.exports = function(options) {
    //cross check options
    options = options || {};
};